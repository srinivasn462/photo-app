<p id="notice"><%= notice %></p>

<p>
  <b>Description:</b>
  <%= @photo.description %>
</p>

<p>
  <b>Photo:</b><br />
  <%= image_tag @photo.photo_url if @photo.photo? %>
</p>

<% if signed_in? %>
<%= link_to "#{@comments.length} comments", photo_comments_path(@photo) %> |
<%= link_to 'Edit', edit_photo_path(@photo) %> |
<% end %>
<%= link_to 'Back', photos_path %>
